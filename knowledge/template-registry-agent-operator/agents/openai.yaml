interface:
  display_name: "Template Registry Agent Operator"
  short_description: "Compile confirmed inputs with English Agent templates"
  default_prompt: "Use $template-registry-agent-operator to select an English Agent template, import sources in any language, confirm decisions, compile, and export a verified prompt package."
