display_name: Yeisme Git Worktree Flow
short_description: Choose safe workspaces, staged checkpoints, Taskfile, and nerdctl compose for development.
default_prompt: Use adaptive workspace routing: keep preview-critical client work in the current checkout, isolate backend hot-reload work in a branch/worktree with separate runtime resources, and verify each staged checkpoint with real project commands.
