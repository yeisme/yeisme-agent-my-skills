# Beat Rubric

每个 beat 以以下字段审查：`goal`、`obstacle`、`choice`、`cost`、`new_information`、`relationship_delta`、`emotion_before`、`emotion_after`、`visual_action`、`next_question`。

如果 `choice` 或 `cost` 缺失，beat 只能标记为 `draft`，不得进入 canonical screenplay 或生产拆解。
