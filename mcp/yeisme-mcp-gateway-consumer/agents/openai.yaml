interface:
  display_name: "Yeisme MCP Gateway Consumer"
  short_description: "Consume local or peer-origin Gateway capabilities."
  default_prompt: "Use $yeisme-mcp-gateway-consumer to connect to the provided Gateway, discover advertised capability origins, and diagnose any scope, approval, or peer-namespace block."
