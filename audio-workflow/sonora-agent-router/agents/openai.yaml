display_name: Sonora Agent Router
short_description: Route Sonora voice, shot-audio intent, native-audio review, and mix workflows to the smallest safe command.
default_prompt: 使用 Sonora 路由一个语音或音频资产任务；若来自视频分镜，先检查版本化 ShotAudioIntent 和原生音频保留/替换策略，再选择最小安全命令。默认用 --agent，只有需要完整嵌套数据时才用 --json。
