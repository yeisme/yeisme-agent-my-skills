interface:
  display_name: "Yeisme Eikona CLI Runtime"
  short_description: "维护 Eikona CLI、run evidence、资产 API 与 provider runtime"
  default_prompt: "Use $yeisme-eikona-cli-runtime to change or review cli/eikona while preserving CLI output, asset lifecycle, OpenAPI, runstore, provider, and verification contracts."
