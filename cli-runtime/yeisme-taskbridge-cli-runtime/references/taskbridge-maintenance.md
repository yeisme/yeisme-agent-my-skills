# TaskBridge Skill Alias Migration

The active maintenance owner is `yeisme-connectors-task-runtime` under `backend-server/connectors`. This reference remains only so older skill links do not fail during the one-release alias window.
