display_name: Local First Backup Sync Policy
short_description: Guide Git-managed local state plus S3/rclone/cloud-drive backup flows without default real-time sync.
default_prompt: Review this local-first project backup plan against Git-managed state plus S3/rclone backup policy.
