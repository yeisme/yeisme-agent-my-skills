# Episode Arc Rubric

审查每集的 `promise`、`question`、`escalation`、`midpoint_change`、`payoff`、`hook`、`character_delta`、`unresolved_debt` 和 `production_risk`。

如果 episode 没有局部 payoff，标记 `needs_rewrite`；如果没有后续问题，标记 `needs_serial_engine`。
