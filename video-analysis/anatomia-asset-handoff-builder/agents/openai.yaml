interface:
  display_name: "Anatomia Asset Handoff Builder"
  short_description: "Prepare a package and target-owner handoff."
  default_prompt: "Use $anatomia-asset-handoff-builder: anatomia revision package prepare <revision> --json then anatomia package handoff prepare <package> --target scaena --json."
