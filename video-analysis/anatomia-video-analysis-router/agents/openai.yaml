interface:
  display_name: "Anatomia Video Analysis Router"
  short_description: "Log in and analyze one authorized local video."
  default_prompt: "Use $anatomia-video-analysis-router: anatomia login --key-file <key> then anatomia analyze file --file <video> --to <out> --json."
