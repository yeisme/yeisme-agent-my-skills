display_name: Yeisme MCP Gateway Maintainer
short_description: Maintain, archive, deploy, and release the Go MCP Gateway with safe credential distribution.
default_prompt: Maintain mcp/gateway, including its Go CLI/API, OpenSpec archive, deployment path, release checks, and least-privilege key lifecycle, while preserving registry-driven routing and secret-safe output.
