interface:
  display_name: "Yeisme MCP Gateway Operator"
  short_description: "Operate revision-safe Gateway admin actions."
  default_prompt: "Use $yeisme-mcp-gateway-operator to inspect the active Gateway revision, create a safe admin plan, and report any required approval."
