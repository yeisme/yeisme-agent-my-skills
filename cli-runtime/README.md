# CLI Runtime Skills

多个 Yeisme CLI 的 runtime 适配集合，是迁移中的临时模块，不是长期产品边界。

Eikona、Pinax、Cohors、Connectors、Quaestor runtime 应逐步迁入对应产品 Skills 仓库；通用 Git worktree 工作流应迁入 `agent-workflow-skills`。迁移时保留 Skill 名称，禁止复制两份。
