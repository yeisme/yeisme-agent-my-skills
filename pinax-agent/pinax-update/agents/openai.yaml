interface:
  display_name: "Pinax Update"
  short_description: "Pull, validate, index, and reconcile Pinax safely"
  default_prompt: "Use $pinax-update to pull the latest Pinax notes, validate the vault, refresh indexes, and stop for unsafe content conflicts."
