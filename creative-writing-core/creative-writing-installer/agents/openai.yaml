interface:
  display_name: "创作 Skill 启用协调器"
  short_description: "通过宿主 capability adapter 按需加载或持久化启用创作 Skill"
  default_prompt: "请使用 $creative-writing-installer 消费既有 SkillActivationPlan，选择最窄 activation scope，先 preview，经当前请求明确授权后再通过宿主 adapter apply，并返回验证和回滚 receipt。"
