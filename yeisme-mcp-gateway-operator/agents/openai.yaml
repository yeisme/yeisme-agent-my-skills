display_name: Yeisme MCP Gateway Operator
short_description: Operate the deployed MCP Gateway through CLI, Web UI, TUI, API, and MCP endpoint checks.
default_prompt: Check the deployed MCP Gateway status, inspect available tools, diagnose degraded backends, and report the next operational command without changing code or registry config.
